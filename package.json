{
  "name": "cert-kokonut-pms",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "root-build": "cd frontend && npm install && npm run build",
    "root-install": "npm install --prefix frontend && npm install --prefix backend"
  }
}
